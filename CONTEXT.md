# 議事録要約・タスク抽出ツール

議事録テキストからAIが要約とタスク一覧を抽出するツールのドメインモデル。

## Language

**議事録要約・タスク抽出ツール**:
このプロジェクトの正式名称。README・ドキュメント等、人が読む文脈での表記はこちらを使う。
_Avoid_: minuts-summarizer（GitHubリポジトリ識別子であり、"minutes"のtypoを含むため正式名称としては使わない）

**期限（Deadline）**:
タスクの締切。現在はGemini APIが返す自由文字列（例:「今週中」「明日まで」）だが、将来的に構造化された日付型に移行予定。「今週中」のような相対的な期限表現は、要約リクエストの実行日ではなく、議事録の会議が行われた日を基準に解決する。
_Avoid_: due date, dueDate（生の自由文字列としての意味合いでは使わない）

**担当者未定**:
議事録内でタスクの担当者が明確に決まっていない状態を表す値。Gemini APIの出力仕様（プロンプト設計）では常に"未定"を使う。
_Avoid_: 不明（`backend/eval/labeling-rules.md` の正解ラベルでは歴史的にこちらが使われており、担当者未定と同一概念。評価スクリプトはこの2つの表記を同一視して比較する）

**タスクマッチング**:
タスク抽出評価（再現率・適合率）において、正解タスク（gold task）とAI出力タスク（output task）が「同じタスクを指しているか」を判定すること。assigneeの一致と、goldタスクに人手で付与したmatchKey（名詞・動詞）がoutputのcontentに含まれるかどうかで判定する。1つのoutputタスクは1つのgoldタスクにのみ対応させる。
_Avoid_: 完全一致（AIによる言い換えで文言は必ず表記ゆれが生じるため、厳密な文字列一致では判定しない）

**deadline正確率**:
タスクマッチングが成立したペアに限定して、deadline（正規化した上で）が一致しているかを測る評価指標。タスクを発見できたかどうか（再現率・適合率）とは独立して集計する。
